#include <stdio.h>
int stack[100];
int top = -1;
int max;
void push(int value) {
    if (top == max - 1) {
        printf("Overflow\n");
    } else {
        stack[++top] = value;
    }
}
void pop() {
    if (top == -1) {
        printf("Underflow\n");
    } else {
        printf("Popped: %d\n", stack[top]);
        top--;
    }
}
void display() {
    if (top == -1) {
        printf("Stack is Empty\n");
    } else {
        printf("Remaining Stack:\n");
        for (int i = top; i >= 0; i--) {
            printf("%d\n", stack[i]);
        }
    }
}
int main() {
    int i, value;
    scanf("%d", &max);
    for (i = 0; i < 3; i++) {
        scanf("%d", &value);
        push(value);
    }
    pop();
    for (i = 0; i < 2; i++) {
        scanf("%d", &value);
        push(value);
    }
    for (i = 0; i < 3; i++) {
        pop();
    }
    display();
    return 0;
}
