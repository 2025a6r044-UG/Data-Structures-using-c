#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,val;
    scanf("%d",&n);
    int list[n];
    for(int i=0;i<n;i++){
        scanf("%d",&list[i]);
    }
    for(int i=n;i>=0;i--){
        list[i+1]=list[i];
    }
    n++;
    scanf("%d",&val);
    list[0]=val;
    for(int i=0;i<n;i++){
        printf("%d ",list[i]);
    }
    return 0;
}
